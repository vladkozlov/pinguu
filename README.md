# Wrapper around ping command for Windows and MacOS

### Extended with functionality to intercept results provided by the ping command output (latency, ttl and etc.)

Example of applying design patterns, regexp, using StreamAPI, new java features like records, instanceOf pattern matching and etc.

Feel free to create issues, ask for features, make PRs.

Requirements:

1. Java 17
2. Maven
3. MacOS/Windows
